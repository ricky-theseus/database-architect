---
name: database-architect
description: >
  Full-stack database architecture expertise — schema design, query optimization,
  indexing strategy, ORM tuning, migration engineering, performance profiling,
  security hardening, and storage engine internals.
license: MIT
metadata:
  author: rick
  version: 1.2.0
  tags: [database, sql, nosql, orm, migration, performance, architecture]
---

This is a symlink-ready copy for OpenCode skill installations.

**Install:**
```bash
cp -r ../.. ~/.config/opencode/skills/database-architect
```

Or symlink (recommended for updates):
```bash
ln -s "$PWD" ~/.config/opencode/skills/database-architect
```

Full reference: see `../../SKILL.md` (18 sections, 595 lines).
